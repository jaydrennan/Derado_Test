apiVersion: v1
kind: Service
metadata:
  name: k8s-dashboard
  namespace: k8s-dashboard
spec:
  type: NodePort
  selector:
    app: k8s-dashboard
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080
