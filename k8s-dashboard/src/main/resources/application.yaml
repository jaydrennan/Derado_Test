server:
  port: 8080

spring:
  application:
    name: k8s-dashboard
  thymeleaf:
    cache: false

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always
